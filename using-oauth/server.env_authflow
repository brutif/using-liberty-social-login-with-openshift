#keystore_password gets created when server gets created.  If keystore file is present, it should not be changed.
keystore_password=vG6Mzv2cweVotY5nDhGJ4fA

# this causes the public keys from /tmp/trustedcert.pem (written by getcerts.sh) to be added to Liberty trust store.
cert_defaultKeyStore=/tmp/trustedcert.pem
